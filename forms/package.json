{
  "name": "myfitcoach-forms",
  "version": "1.0.0",
  "private": true,
  "description": "MyFitCoach Forms - standalone client-facing forms service (forms.myfitcoach.ie). Shares the Postgres database with the coach portal but has no code dependency on it.",
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js",
    "generate-links": "node db/generate-links.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
